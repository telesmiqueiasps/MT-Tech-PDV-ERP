# PDV ERP — dependências externas
# Python >= 3.11  (usa tkinter da stdlib, sqlite3 da stdlib)

# Manipulação de imagens (logo, assets visuais)
Pillow>=10.1.0

# Geração de PDF (relatório de caixa, DANFE, cupom)
reportlab>=4.2.5

# Servidor web para app garçom (Wi-Fi local)
flask>=3.0.0
flask-cors>=4.0.0

# QR Code (tela de mesas — link do app garçom)
qrcode[pil]>=8.0

# NFC-e — Emissão de Nota Fiscal de Consumidor Eletrônica
nfelib==2.5.1
erpbrasil.assinatura>=1.8.0
lxml>=5.2.1
cryptography>=42.0.5
requests>=2.31.0
pyOpenSSL>=24.0.0
