Responda sempre em português do Brasil.
# PDV ERP

- BaseView herda de tk.Toplevel (não é Frame, não usa .pack())
- Session.usuario(), Session.empresa() — sem método .get()
- Banco mestre: DatabaseManager.master()
- Banco empresa: DatabaseManager.empresa()
- Tabela empresas tem campos: razao_social, cnpj, endereco, numero, bairro, cidade, estado